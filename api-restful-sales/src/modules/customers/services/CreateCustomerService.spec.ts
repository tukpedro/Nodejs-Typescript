describe('Create Customer', () => {
  it('should create a new customer', () => {
    expect(1).toBe(1);
  });

  it('should NOT create a new customer with existent email', () => {
    expect(1).toBe(1);
  });
});
